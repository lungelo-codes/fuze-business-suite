import { NextResponse } from "next/server";
import { cookies } from "next/headers";
import { erpGet, erpPatch } from "@/lib/server/erpnext";
interface UserProfile { name?: string; full_name?: string; email?: string; mobile_no?: string; phone?: string; }
export async function GET() { try { const cookieStore = await cookies(); const userId = cookieStore.get("user_id")?.value; if (!userId) return NextResponse.json({ error: "Not authenticated" }, { status: 401 }); const doc = await erpGet<{ data?: UserProfile; message?: UserProfile }>(`/api/resource/User/${encodeURIComponent(userId)}`); return NextResponse.json({ user: doc.data ?? doc.message ?? {} }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not fetch profile" }, { status: 500 }); } }
export async function PATCH(req: Request) { try { const body = await req.json() as Record<string, unknown>; const update: Record<string, unknown> = {}; for (const key of ["full_name","mobile_no","phone"]) if (body[key] !== undefined) update[key] = body[key]; if (!Object.keys(update).length) return NextResponse.json({ error: "No valid fields to update" }, { status: 400 }); const cookieStore = await cookies(); const userId = cookieStore.get("user_id")?.value; if (!userId) return NextResponse.json({ error: "Not authenticated" }, { status: 401 }); const updated = await erpPatch<UserProfile>("User", userId, update); return NextResponse.json({ success: true, user: updated }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not update profile" }, { status: 500 }); } }
